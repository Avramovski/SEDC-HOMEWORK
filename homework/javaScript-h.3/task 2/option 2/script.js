console.log('task 2');

// with prompt & alert

let years = prompt("vnesi kolku godini ima kuceto");

function calculateDogAge(age) {
    let dogAge = 7 * age ;  
    return dogAge;
    }

    alert((`Vo covecki dodini ima ${calculateDogAge(years)} godini`));